<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Resolution_Comments</fullName>
    <active>true</active>
    <description>Displays error if resolution comments are added when Case Status is not &#39;c</description>
    <errorConditionFormula>AND(NOT(ISBLANK(FS_Resolution_Comments__c)),
    NOT(ISPICKVAL( Status , &#39;Closed&#39;)))</errorConditionFormula>
    <errorMessage>Resolution Comments are not allowed unless case is being closed</errorMessage>
</ValidationRule>
